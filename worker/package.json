{
  "name": "lifestream-api-worker",
  "version": "1.0.0",
  "description": "Cloudflare Workers port of the Lifestream Strava API (hono + D1 + Prisma)",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "prisma:generate": "prisma generate"
  },
  "dependencies": {
    "@prisma/adapter-d1": "^6.1.0",
    "@prisma/client": "^6.1.0",
    "date-fns": "^3.6.0",
    "hono": "^4.6.0",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "prisma": "^6.1.0",
    "wrangler": "^4.0.0"
  }
}
